server:
  port: 80

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
    loadbalancer:
      retry:
        enabled: true

# 聚合文档配置
hert:
  document:
    resources:
      - name: 授权模块
        location: /hert-auth
      - name: 基础模块
        location: /hert-base


