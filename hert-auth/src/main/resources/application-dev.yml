#服务器端口
server:
  port: 8100

#数据源配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${hert.datasource.dev.url}
    username: ${hert.datasource.dev.username}
    password: ${hert.datasource.dev.password}
